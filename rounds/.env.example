# Optional public read-only catalog URL. Blank uses the bundled preview.
# Serve the full/delta JSON envelope documented in docs/app-guide.md.
# Never put private keys or service account credentials in EXPO_PUBLIC variables.
EXPO_PUBLIC_CONTENT_API_URL=

# Optional development-only preview. Ignored in production builds.
EXPO_PUBLIC_PREVIEW_CONTENT_API_URL=
